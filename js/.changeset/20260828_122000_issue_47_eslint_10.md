---
'lino-objects-codec': patch
---

Update ESLint from 9 to 10 and add `@eslint/js` as an explicit dev dependency. ESLint 10 no longer installs it as a transitive dependency, so `eslint.config.js`, which has always imported it, would otherwise fail to load (issue #47).
